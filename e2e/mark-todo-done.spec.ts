import { test } from "@playwright/test";

/**
 * BA workshop — fill in this test (pair with your AI).
 *
 * Story: After sign-in, the dashboard lists a sample todo. I check the box to
 * mark it complete, and I should see that same title under the “Done” section
 * (and it should no longer appear under “To do”).
 *
 * Run: `npm run test:e2e`
 * Optional reference: `e2e/mark-todo-done.answer-key.spec.ts` or `npm run test:e2e:answer`
 */
test("mark sample todo complete moves it to Done section", async ({ page }) => {
  throw new Error(
    "Workshop: implement this test with your AI assistant, then delete this line.",
  );
});
